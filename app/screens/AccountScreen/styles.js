import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    input: {
        height: 40,
        borderColor: 'blue',
        borderWidth: 2,
        padding: 10,
        margin: 10,
        width: 200
    }
});